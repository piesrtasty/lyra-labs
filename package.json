{
  "name": "lyra-labs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "generate": "docker exec -it frontend npm run generate",
    "start": "docker-compose up",
    "build": "docker-compose build",
    "stop": "docker-compose down",
    "clean": "docker system prune -af",
    "clean:volumes": "docker volume prune -f",
    "seed": "docker exec -it prisma npm run seed",
    "ci:deploy:staging": "now -A staging.now.json --target=staging --token=$NOW_TOKEN --scope=piesrtasty  && now alias -A staging.now.json  --token=$NOW_TOKEN",
    "ci:deploy:prod": "now -A prod.now.json --token=fFIRtUzRl3QKETo9r1nQ0IPt --scope=piesrtasty"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "10"
  }
}
